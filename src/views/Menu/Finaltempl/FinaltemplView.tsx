type dataType = {
    clickPb: boolean,
        clickMakeup: boolean,
        clickMeal: boolean,
        clickAdd: string[],
}

const FinaltemplView = (e: dataType) => {
  console.log(e)
  return (
    <div>보여줘</div>
  )
}

export default FinaltemplView